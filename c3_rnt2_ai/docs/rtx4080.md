# RTX 4080 16GB: ruta recomendada

<!-- markdownlint-disable MD013 -->

## Install

```bash
python -m venv .venv
.venv\\Scripts\\activate
pip install -e .
pip install -e .[api]
pip install -e .[hf,train]   # opcional (HF + QLoRA)
```

## Doctor (sin bajar pesos HF)

```bash
python -m c3rnt2 doctor --profile rtx4080_16gb_vortexx --deep
python -m c3rnt2 doctor --profile qwen8b_base --deep
```

## Chat / Serve

```bash
python -m c3rnt2 chat  --profile rtx4080_16gb_vortexx
python -m c3rnt2 serve --profile rtx4080_16gb_vortexx

python -m c3rnt2 chat  --profile qwen8b_base
python -m c3rnt2 serve --profile qwen8b_base
```

## Bench (unificado)

```bash
python -m c3rnt2 bench --profile rtx4080_16gb_vortexx --max-new-tokens 64
python -m c3rnt2 bench --profile qwen8b_base --max-new-tokens 64
```

## Self-train (serve-self-train)

HF (QLoRA):

```bash
python -m c3rnt2 serve-self-train --profile rtx4080_16gb_safe_hf --host 0.0.0.0 --port 8000
```

Core (VORTEX):

```bash
python -m c3rnt2 serve-self-train --profile safe_selftrain_4080 --host 0.0.0.0 --port 8000
```

## Autopilot (internet + autopatch seguro)

Perfil recomendado (gated por approval file):

```bash
python -m c3rnt2 serve-autopilot --profile autonomous_4080_hf --host 0.0.0.0 --port 8000
```

Para permitir aplicar/mergear parches (por defecto **NO** aplica nada):

```bash
type NUL > data\\APPROVE_AUTOPATCH
```

Runner con rollback automático (si autopilot pide restart o falla):

```bash
python scripts\\run_daemon.py --profile autonomous_4080_hf --host 0.0.0.0 --port 8000
```
