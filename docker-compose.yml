name: vortex

x-backend-build: &backend-build
  context: ./c3_rnt2_ai
  dockerfile: Dockerfile
  args:
    VORTEX_BACKEND_IMAGE: ${VORTEX_BACKEND_IMAGE:-nvidia/cuda:12.8.1-cudnn-devel-ubuntu24.04}
    LLAMA_CPP_CMAKE_ARGS: ${LLAMA_CPP_CMAKE_ARGS:--DGGML_CUDA=on -DCMAKE_CUDA_ARCHITECTURES=89}
    LLAMA_CPP_PIP_EXTRA_INDEX_URL: ${LLAMA_CPP_PIP_EXTRA_INDEX_URL:-}
    VORTEX_INSTALL_TRAINING: ${VORTEX_INSTALL_TRAINING:-1}

x-backend-env: &backend-env
  C3RNT2_PROFILE: ${C3RNT2_PROFILE:-rtx4080_16gb_llama2_7b_q4_local}
  NVIDIA_VISIBLE_DEVICES: ${NVIDIA_VISIBLE_DEVICES:-all}
  NVIDIA_DRIVER_CAPABILITIES: ${NVIDIA_DRIVER_CAPABILITIES:-compute,utility}
  C3RNT2_HOST_WORKSPACE_REPO_NAME: Vortex
  C3RNT2_HOST_WORKSPACE_WINDOWS_ROOT: ${C3RNT2_HOST_WORKSPACE_WINDOWS_ROOT:-D:\GitHub\Vortex}
  C3RNT2_HOST_WORKSPACE_MOUNT: /workspace/Vortex
  C3RNT2_HOST_D_WINDOWS_ROOT: ${C3RNT2_HOST_D_WINDOWS_ROOT:-D:\GitHub}
  C3RNT2_HOST_D_MOUNT: /workspace/GitHub
  C3RNT2_HOST_DOWNLOADS_WINDOWS_ROOT: ${C3RNT2_HOST_DOWNLOADS_WINDOWS_ROOT:-C:\Users\marcm_kntz5zt\Downloads}
  C3RNT2_HOST_DOWNLOADS_MOUNT: /host/downloads
  VORTEX_API_HOST: 0.0.0.0
  VORTEX_API_PORT: 8000
  HF_HOME: /root/.cache/huggingface
  TRANSFORMERS_CACHE: /root/.cache/huggingface
  PYTHONUNBUFFERED: "1"

x-gpu: &gpu
  gpus: all
  deploy:
    resources:
      reservations:
        devices:
          - driver: nvidia
            count: all
            capabilities: [gpu]

services:
  vortex-api:
    <<: *gpu
    image: vortex-backend:local
    build: *backend-build
    environment: *backend-env
    command:
      - python
      - -m
      - c3rnt2
      - serve
      - --profile
      - ${C3RNT2_PROFILE:-rtx4080_16gb_llama2_7b_q4_local}
      - --host
      - 0.0.0.0
      - --port
      - "8000"
    ports:
      - "${VORTEX_API_PORT:-8000}:8000"
      - "${VORTEX_FLUTTER_WEB_PORT:-19090}:19090"
    volumes:
      - .:/workspace/Vortex
      - ./c3_rnt2_ai/src:/app/src
      - ./c3_rnt2_ai/config:/app/config
      - ${C3RNT2_HOST_D_WINDOWS_ROOT:-D:\GitHub}:/workspace/GitHub
      - ${C3RNT2_HOST_DOWNLOADS_WINDOWS_ROOT:-C:\Users\marcm_kntz5zt\Downloads}:/host/downloads
      - ${VORTEX_HF_CACHE_WINDOWS_ROOT:-C:\Users\marcm_kntz5zt\.cache\huggingface}:/root/.cache/huggingface
      - vortex-frontend-node-modules:/workspace/Vortex/vortex-chat/node_modules
      - ./c3_rnt2_ai/data/models/gguf:/app/data/models/gguf:ro
      - vortex-chat-data:/app/data/chat
      - vortex-support-data:/app/data/support_chatbot
      - vortex-episodes-data:/app/data/episodes
      - vortex-logs-data:/app/data/logs
      - vortex-web-cache:/app/data/web_cache
      - vortex-self-patch:/app/data/self_patch
      - vortex-workspaces:/app/data/workspaces
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/readyz', timeout=5)"]
      interval: 20s
      timeout: 10s
      start_period: 180s
      retries: 12
    restart: unless-stopped
    networks:
      - vortex-net

  vortex-control:
    <<: *gpu
    image: vortex-backend:local
    build: *backend-build
    environment:
      <<: *backend-env
      VORTEX_CONTROL_HOST: 0.0.0.0
      VORTEX_CONTROL_PORT: 8765
    command:
      - python
      - -m
      - c3rnt2.control_server
      - --base-dir
      - /app
      - --compose-file
      - /workspace/Vortex/docker-compose.yml
      - --host
      - 0.0.0.0
      - --port
      - "8765"
      - --api-url
      - http://vortex-api:8000
      - --frontend-url
      - http://vortex-chat:5173
      - --disable-compose-actions
      - --assume-docker-ready
    ports:
      - "${VORTEX_CONTROL_PORT:-8765}:8765"
    volumes:
      - .:/workspace/Vortex
      - ./c3_rnt2_ai/src:/app/src
      - ./c3_rnt2_ai/config:/app/config
      - ${C3RNT2_HOST_D_WINDOWS_ROOT:-D:\GitHub}:/workspace/GitHub
      - ${C3RNT2_HOST_DOWNLOADS_WINDOWS_ROOT:-C:\Users\marcm_kntz5zt\Downloads}:/host/downloads
      - ${VORTEX_HF_CACHE_WINDOWS_ROOT:-C:\Users\marcm_kntz5zt\.cache\huggingface}:/root/.cache/huggingface
      - vortex-frontend-node-modules:/workspace/Vortex/vortex-chat/node_modules
      - ./c3_rnt2_ai/data/models/gguf:/app/data/models/gguf:ro
      - vortex-chat-data:/app/data/chat
      - vortex-support-data:/app/data/support_chatbot
      - vortex-episodes-data:/app/data/episodes
      - vortex-logs-data:/app/data/logs
      - vortex-web-cache:/app/data/web_cache
      - vortex-self-patch:/app/data/self_patch
      - vortex-workspaces:/app/data/workspaces
    depends_on:
      vortex-api:
        condition: service_started
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8765/healthz', timeout=5)"]
      interval: 20s
      timeout: 10s
      start_period: 20s
      retries: 6
    restart: unless-stopped
    networks:
      - vortex-net

  vortex-chat:
    image: vortex-chat:local
    build:
      context: ./vortex-chat
      dockerfile: Dockerfile
    ports:
      - "${VORTEX_FRONTEND_PORT:-5173}:5173"
    depends_on:
      vortex-api:
        condition: service_started
      vortex-control:
        condition: service_started
    healthcheck:
      test: ["CMD", "wget", "-q", "-O", "/dev/null", "http://127.0.0.1:5173/"]
      interval: 15s
      timeout: 5s
      start_period: 10s
      retries: 5
    restart: unless-stopped
    networks:
      - vortex-net

networks:
  vortex-net:
    driver: bridge

volumes:
  vortex-chat-data:
  vortex-support-data:
  vortex-episodes-data:
  vortex-logs-data:
  vortex-web-cache:
  vortex-self-patch:
  vortex-workspaces:
  vortex-frontend-node-modules:
